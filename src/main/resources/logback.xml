<configuration>

    <!-- Define appenders -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define loggers -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Set specific log levels for packages or classes -->
    <logger name="com.tms.springjdbc.infrastructure" level="debug"/>
    <logger name="com.tms.springjdbc.application" level="info"/>
    <logger name="com.tms.springjdbc.presentation" level="info"/>
    <logger name="com.tms.springjdbc.infrastructure.aop" level="debug"/>

</configuration>
