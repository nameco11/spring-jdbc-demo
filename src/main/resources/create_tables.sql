CREATE TABLE cars (
                      id INT AUTO_INCREMENT  PRIMARY KEY,
                               name VARCHAR(100) ,
                               brand VARCHAR(100),
                               groupId int,
                               color VARCHAR(100),
                               type VARCHAR(100),
                      quantity int,
                      price decimal
);